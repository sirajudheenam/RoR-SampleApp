%h1 Listing instruments array

= link_to 'New Instrument', new_instrument_path, {class: 'btn btn-primary pull-right'}

%table.table.table-striped
  %thead
    %tr
      %th ID
      %th Title
      %th Token
      %th Count
      %th{span: 3}


  %tbody
    - @sorted_instruments.each do |instrument|
      %tr
        %td= instrument[:id]
        %td= instrument[:title]
        %td= instrument[:token]
        %td= instrument[:count]
        %td= link_to 'Show', instrument, {class: 'btn btn-primary'}
        %td= link_to 'Edit', edit_instrument_path(instrument), {class: 'btn btn-secondary'}
        %td= link_to 'Destroy', instrument, method: :delete, class: 'btn btn-warning', data: { confirm: 'Are you sure?' }

%br

